SPAWN_RATE = 5

spawner = {
    spawntime = 0,
}

function update_spawner()
    
    -- if (spawner.spawntime < time()) then
    --     local tree_c = grid[13][2]
    --     create_tree(tree_c.x, tree_c.y)

    --     local sider_c = get_rnd_valid_grid_cell()
    --     create_sider(sider_c.x, sider_c.y)

    --     spawner.spawntime += SPAWN_RATE

    -- end
end